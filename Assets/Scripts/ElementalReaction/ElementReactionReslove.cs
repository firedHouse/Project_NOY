using UnityEngine;

public static class ElementReactionResolver
{/*
    // 원소들이 만나서, 원소 반응이 일어나는걸 판별하는 Resolve 함수
    public static ElementReaction Resolve(ElementType current, ElementType attack)
    {
        // 비트연산자를 이용하여, 커런트와 어택의 원소반응을 조합(판별)함
        ElementType mix = current | attack;

        if (mix == (ElementType.Fire | ElementType.Water))
        {
            return ElementReaction.Vaporize;
        }
        if (mix == (ElementType.Water | ElementType.Electric))
        {
            return ElementReaction.ElectroShock;
        }
        if (mix == (ElementType.Fire | ElementType.Electric))
        {
            return ElementReaction.Overload;
        }

        return ElementReaction.None;

  } */
}
